const temIngresso = false
// para negar uma afirmação utilize "!" antes da variável a ser verificada dentro do IF

if (!temIngresso) {
    console.log("Pessoa sem ingresso.");
}
console.log(`variável temIngresso: ${temIngresso}`);
